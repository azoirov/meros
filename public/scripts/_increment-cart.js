import { selectOne, selectAll } from "./_functions";

export default function () {
    try {
        const incrementBtns = selectAll(".product-card__cart__btn--increment");
        incrementBtns.forEach((btn) => {});
    } catch (e) {}
}
